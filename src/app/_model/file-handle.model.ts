import { SafeUrl } from "@angular/platform-browser";

export interface FileHandle {
    // picByte(picByte: any, type: any): unknown;
    // type(picByte: any, type: any): unknown;
    // name: string;
    // file: File;
    // url: SafeUrl| string;
}